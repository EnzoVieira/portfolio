import { notFound } from "next/navigation";

// TODO: Implement metadata for SEO

export default async function InvlancePage() {
  if (!true) {
    return notFound();
  }

  return <main>página do projeto</main>;
}
